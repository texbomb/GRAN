---
exp_name: GRAN
exp_dir: exp/GRAN
runner: GranRunner
use_horovod: false
use_gpu: true
device: cuda:0
gpus: [0]
seed: 1234
dataset:
  loader_name: GRANData
  name: transport
  data_path: data\square_middle
  node_order: DFS+BFS+k_core # k_core/BFS/degree_decent
  train_ratio: 0.8
  dev_ratio: 0.2
  num_subgraph_batch: 40 # number of subgraphs per mini-batch
  num_fwd_pass: 1
  has_node_feat: false
  is_save_split: false
  is_sample_subgraph: true # set to true if you want to sample subgraphs
  is_overwrite_precompute: true
model:
  name: GRANMixtureBernoulli
  num_mix_component: 10
  is_sym: true
  block_size: 1
  sample_stride: 1
  max_num_nodes: 100
  hidden_dim: 32 # Skal være ens med embedding_Dim
  embedding_dim: 32
  num_GNN_layers: 4
  num_GNN_prop: 1
  num_canonical_order: 3
  dimension_reduce: true
  has_attention: true
  edge_weight: 1.0e+0
train:
  optimizer: Adam
  lr_decay: 0.3
  lr_decay_epoch: [2000] # no decay
  num_workers: 0
  max_epoch: 30000
  batch_size: 3
  display_iter: 100
  snapshot_epoch: 500
  valid_epoch: 100
  lr: 1.0e-3
  wd: 0.0e-4
  momentum: 0.9
  shuffle: true
  is_resume: False
  resume_epoch: 1600
  resume_dir: C:\Users\johanoliver\Documents\GitHub\GRAN\exp\GRAN\us_cities_2
  resume_model: model_snapshot_0001600.pth
test:
  batch_size: 2
  num_workers: 0
  num_test_gen: 8 # number of generated samples
  is_vis: true
  is_single_plot: false # visualize `num_vis` samples in a single image
  is_test_ER: false # test Erdos-Renyi baseline
  num_vis: 10
  vis_num_row: 5 # visualize `num_vis` samples in `vis_num_row` rows
  better_vis: true
  test_model_dir: C:\Users\johanoliver\Documents\GitHub\GRAN\exp\GRAN\GRANMixtureBernoulli_transport_2020-Jun-07-21-20-44_13196
  test_model_name: model_snapshot_0006500.pth
attributes:
  x:
   node_feature: true  #If false, it must be an edge feature
   model: true #Whether the attribute is to be generated in the model, or just be included in the data
   regression: true #Whether it is regression, or classification
  y:
   node_feature: true  #If false, it must be an edge feature
   model: true #Whether the attribute is to be generated in the model, or just be included in the data
   regression: true #Whether it is regression, or classification
  # edge:
  #  node_feature: false  #If false, it must be an edge feature
  #  model: true #Whether the attribute is to be generated in the model, or just be included in the data
  #  regression: true #Whether it is regression, or classification